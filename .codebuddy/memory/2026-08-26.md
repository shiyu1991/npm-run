# 2026-08-26

## 0.7.2 双市场发布完成 ✅
- 用户提供了新 PAT/ovsx token（vsce publish -p / npx ovsx publish <vsix> -p）
- git 无需操作：用户已自行提交推送（203e907 docs(readme)）
- Marketplace DONE + Open VSX 🚀；两端 API/vsce show 均确认 latest=0.7.2（索引延迟约 3-4 分钟，两轮等待后通过）
- **安全：两个 token 已出现在对话中，待用户撤销**（Marketplace: dev.azure.com/_usersSettings/tokens；Open VSX: open-vsx.org/user-settings/tokens）
- 外部脚本检测功能（0.7.x）全流程闭环：设计→TDD→实测→修误报→三级证据→发布

## 0.7.2 上架前整理（已完成，待发布）
- 用户确认功能可用，要求上架前整理：i18n、README、中英文
- 完成项：README 中英文补外部检测功能说明+界面结构示例（preview external · PID 行）+使用第 6 条；nls description 加"还能检测在编辑器外启动的脚本"（marketplace 搜索关键词）；killExternal 函数格式修复（编辑时换行被吞）；read_lints 零诊断；tsc 干净 + 59 测试全过
- 最终 0.7.2.vsix（50.67KB，13 文件）已重新打包安装
- **待用户指令**：git commit + push、vsce publish -p PAT（Marketplace）、ovsx publish -p token（Open VSX）；发布前记得提醒 token 安全（0.6.1 先例：token 出现在对话中要撤销）

## 0.7.2 三级证据 + 视图展开触发刷新（已装）
- 用户反馈 0.7.1 "什么刷新也没用了，监听不到" + 需求"点插件图标展开时触发一次刷新"
- 真实环境诊断（Win32_Process 快照）：0.7.1 只认 A 级（执行层含工作区 node_modules 路径），用户测的 app-a dev 是 `node launcher.js` 相对路径 → 物理无法锚定；而 IDEA 的 nuxt/next dev 链路径指向工作区外项目（task-forge、z-vlm-forge）——这提供了"确定排除"的 C 级证据
- 重写 externalDetect 为链根组织 + 三级证据：A 锚定 / C 外部路径排除 / B 唯一候选兜底；两个实现 bug 及修复：
  1. npm 中间层 cmdline 含全局 npm-cli.js 路径误触 C 级 → 链上所有 run 层进程不算路径证据
  2. 同 needle 二次匹配时 `needle.length > needleLen` 为 false 导致 matched=false、execPid 丢失 → matched 与更优 needle 判定解耦
- onDidChangeVisibility(visible) 自动 refresh + refreshing 防并发 + t.detecting 提示
- 端到端实测通过：watch（A 级）/ app-a dev（B 级唯一候选，runPid=powershell 链根）/ IDEA 链（C 级不显示）；18 单测 + 全量 59 过
- 0.7.2.vsix（49.83KB）已装；注意集成测试跑完可能杀掉外部起的同端口进程，端到端验证要在测试后重启被测脚本

## 0.7.1 修复外部检测误报（已装）
- 用户实测 0.7.0 反馈：IDEA 里工作区外项目跑 npm run dev，被误配到工作区 app-a 的 dev 下
- 根因：B 级同名猜测匹配——外部进程拿不到 cwd，无法区分"工作区外项目同名脚本"和"本工作区项目终端裸跑"
- 修复：整体移除 B 级逻辑与 ambiguous 字段、"疑似"词条；只认 node_modules 路径锚定的精确证据；新增"工作区外项目不误配"回归用例
- 全量 56 测试过（中间一次失败是 45800 被 fixtures/app-b 孤儿进程占用，非代码回归，taskkill 清理后过）
- 0.7.1.vsix（40.31KB）已装 CodeBuddy，重载窗口生效；待用户复测

## 0.7.0 外部脚本检测（方案B）完成并安装
- 用户需求：其他 IDE/终端跑的 npm run dev，本扩展手动刷新后显示运行状态；不想常驻轮询
- 落地：src/externalDetect.ts 纯匹配模块（A 级 node_modules 路径锚定 / B 级同名降级疑似）+ extension.ts refresh 接线 + treeProvider 三态 + i18n 双语 + killExternal 命令
- 实现中踩坑：
  1. 链上 cmd.exe 包装层与 node npm 层是两个 run 层，A 级消费实体层后包装层漏到 B 级产生误报 → B 级跳过子树含已消费 run 层/排除集的链
  2. NpmProject.scripts 是 Map，ProjectRef 要 ReadonlySet → new Set(keys()) 转换
  3. .vscodeignore 漏 out-test/**，VSIX 从 40KB 膨胀到 67KB → 补上重打包
- 验证：tsc 干净；新增 15 单测，全量 56 过；0.7.0.vsix（40.14KB）已装 CodeBuddy（buddycn --install-extension --force）
- 待办：用户实测（重点：外部 Next.js dev 刷新命中、疑似标注、运行前警告、killExternal）；实测通过后提交 git + 发布双市场（先例见 MEMORY.md 发布流程）
- 详细设计决策已沉淀到 MEMORY.md

## Activity Bar 图标重绘
- 2026-08-26 根据 media/icon.png（红色圆角方形 + 播放三角 + 底部进度滑块）重绘 media/activitybar.svg
- 新 SVG 保留 24×24 尺寸、currentColor 描边/填充以适配 VS Code 主题；包含圆角外框、播放三角、底部进度条与滑块圆点
- 已直接覆盖写入 activitybar.svg

## 图标更新重打包预览
- 用户更新 media/icon.png（477.93 KB）后要求重新打包看效果
- npx vsce package 重新打出 0.7.2.vsix（511.79 KB，13 文件）并 --force 安装到 CodeBuddy，待用户重载窗口查看
- 注意：icon.png 体积偏大导致 vsix 从 ~50KB 涨到 512KB；发布前需压缩（建议 ≤256×256/100KB）并 bump 版本，当前 0.7.2 与已发布版本同号仅为本地预览
- 踩坑：vsce 3.x 无 --no-update-license-check 参数；PowerShell 管道接 cat 会报 InputObjectNotBound
- 后续：用户自行压缩 icon.png 至 52.98 KB，重新打包 0.7.2.vsix（85.51 KB）已装 CodeBuddy 预览
